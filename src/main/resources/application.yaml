spring:
  application:
    name: Hookah-Order-System
  datasource:
    url: jdbc:postgresql://localhost:5432/hos_db
    username: postgres
    password: 130997
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
server:
  port: 8081